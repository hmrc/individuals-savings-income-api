get:
  $ref: "./savings_uk_accounts_list.yaml"
  summary: List All UK Savings Accounts
  security:
    - User-Restricted:
        - read:self-assessment
  parameters:
    - $ref: './common/queryParameters.yaml#/components/parameters/savingsAccountId'

post:
  $ref: "./savings_uk_accounts_add.yaml"
  summary: Add a UK Savings Account
  security:
    - User-Restricted:
        - write:self-assessment

put:
  # {{#if (enabled 'update-uk-savings-account-name')}}
  $ref: "./savings_uk_accounts_name_update.yaml"
  summary: Update a UK Savings Account name
  security:
    - User-Restricted:
        - write:self-assessment
  # {{/if}}
